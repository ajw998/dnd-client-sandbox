import { h } from 'preact';
import { memo } from 'preact/compat';
import { useAppSelector, useAppDispatch } from '../../../store/hooks';

export const AttackDamageTotal = () => {

}

export default memo(AttackDamageTotal);
